<h1>Listing comments</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th>Content</th>
  </tr>

<% @plugin.comments.each do |comment| %>
  <tr>
    <td><%= comment.name %></td>
    <td><%= comment.email %></td>
    <td><%= comment.content %></td>
  </tr>
<% end %>
</table>

<br />

