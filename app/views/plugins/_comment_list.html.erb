<ul id="comment-list">
  <% @plugin.comments.each do |comment| %>
  <li>
    <div class="meta">written <%= time_ago_in_words comment.created_at %> ago by <%= h comment.name %></div>
    <div class="content"><%= h comment.content %></div>
  </li>
  <% end %>
</ul>
